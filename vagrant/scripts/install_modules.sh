# Script to download dependencies as Puppet modules

# puppet module install <author>/<module> --modulepath $1 --version <version-number>

# yum install -y git
# git clone <repo-url> $1/mymodule

exit 0